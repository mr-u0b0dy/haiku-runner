#include <zephyr/logging/log.h>

LOG_MODULE_REGISTER(audio_pipeline_sof, LOG_LEVEL_INF);

int audio_pipeline_sof_adapter_note(void)
{
  LOG_INF("SOF adapter path selected (placeholder)");
  return 0;
}
