# All three real inputs registered together on the stub backend, to exercise
# source_manager's arbitration and auto-fallback across inputs rather than
# any one input's audio path.
CONFIG_HR_INPUT_AUX=y
CONFIG_HR_INPUT_USB=y
CONFIG_HR_INPUT_USB_UAC2=y
